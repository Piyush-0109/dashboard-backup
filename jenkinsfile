pipeline {
    agent any

    stages {
        stage('Installing Requirements') {
            steps {
                sh '''
                pip install -r requirement.txt
                '''
            }
        }
        stage('Running Backup') {
            steps {
                sh '''
                python3 dashboard_backup.py
                '''
            }
        }
    }
}
